# EZ1012 Invalid\_Argument

## 错误信息

报错格式如下，占位符%s的含义依次为参数值、参数名、参数最大值：

```text
Value %s for parameter %s is out of range, it should be smaller than %s.
```

报错示例如下：

```text
Value 98 for parameter index is out of range, it should be smaller than 1.
```

## 解决方法

根据报错提示调整参数值。
