# OP\_ATTR

## 宏功能

用于封装算子的属性参数。

## 宏原型

```cpp
OP_ATTR(x...)
```

## 参数说明

| 参数 | 输入/输出 | 说明 |
| --- | --- | --- |
| x... | 输入 | 算子的属性参数（即算子原型中属性参数）。 |

## 约束说明

无

## 调用示例

```cpp
// 封装算子的2个属性参数，dstType和sqrtMode
OP_ATTR(dstType, sqrtMode);
```
