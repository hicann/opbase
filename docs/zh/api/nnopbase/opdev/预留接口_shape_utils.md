# 预留接口

本章接口为预留接口，后续有可能变更或废弃，不建议开发者使用，开发者无需关注。

**表 1**  接口列表

| 接口定义 | 功能说明 |
| --- | --- |
| ToString(const op::Shape &shape) | 将op::Shape转换为ge::AscendString的字符串。 |
| ToString(const op::Strides &strides) | 将op::Strides转换为ge::AscendString的字符串。 |
