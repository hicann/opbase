# Reserved APIs

The APIs described in this section are reserved and may be changed or deprecated in the future. Therefore, you are not advised to pay attention to or use these APIs.

**Table 1** API list

| API Definition| Description|
| --- | --- |
| ToString(const op::Shape &shape) | Converts `op::Shape` to `ge::AscendString`.|
| ToString(const op::Strides &strides) | Converts `op::Strides` to `ge::AscendString`.|
